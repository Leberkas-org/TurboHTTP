# Plan: RFC 9114 (HTTP/3) — 99% Client Compliance

## Introduction

RFC 9114 has 145 client-side MUSTs and is currently at 1.4% (2/145). This plan defines the complete HTTP/3 client implementation in ~51 atomic tasks across 9 phases. Target: >=99% (143/145).

**Key architecture insight**: TurboHttp's transport layer is already abstracted via `IClientProvider`. No `QuicConnectionStage` is needed — the existing `ConnectionStage`, `ClientByteMover`, `ClientState`, and actor hierarchy work unchanged. Only a `QuicClientProvider` and `QuicOptions` are needed alongside the HTTP/3 protocol layer.

**Included in this plan**: This plan includes the QPACK header compression codec (RFC 9204) as Phase 4, which is a prerequisite for HTTP/3 header encoding/decoding. All 13 QPACK tasks are merged here with full detail.

## Goals

- RFC 9114 client compliance from 1.4% to >=99% (143/145)
- Complete HTTP/3 frame codec and QPACK header compression
- QUIC integration via `QuicClientProvider` implementing `IClientProvider`
- `Http30Engine` as `IHttpProtocolEngine` in the existing Engine demuxer
- ~280 new tests with RFC-tagged DisplayNames

## Transport Architecture (No QuicConnectionStage Needed)

The existing transport abstraction already supports QUIC with minimal changes:

```
IClientProvider (existing interface)
├── TcpClientProvider  → NetworkStream     (existing)
├── TlsClientProvider  → SslStream         (existing)
└── QuicClientProvider → QuicStream         (NEW — only new transport code)

Transport selection point: ClientManager.Handle() — 1 line change
Options selection point:   TcpOptionsFactory.Build() — ~10 lines
```

**What stays unchanged**: `ConnectionStage`, `ClientByteMover`, `ClientState`, `ConnectionHandle`, `ConnectionActor`, `HostPool`, `PoolRouter` — all transport-agnostic.

## User Stories

### PHASE 1 — Foundation (Week 1-2)

---

### TASK-001: QuicOptions Record

**Description:** As a library developer, I want a `QuicOptions` record extending `TcpOptions` so that QUIC-specific settings are available.

**Implementation**:
- New file: `src/TurboHttp/Transport/QuicOptions.cs`
  - `public record QuicOptions : TcpOptions` with QUIC-specific fields:
    - `TimeSpan IdleTimeout { get; init; } = TimeSpan.FromSeconds(30)`
    - `int MaxBidirectionalStreams { get; init; } = 100`
    - `int MaxUnidirectionalStreams { get; init; } = 3`
    - `List<SslApplicationProtocol> ApplicationProtocols { get; init; } = [new("h3")]`

**Acceptance Criteria:**
- [x] QuicOptions inherits all TcpOptions properties (Host, Port, ConnectTimeout, etc.)
- [x] QUIC-specific properties have sensible defaults
- [x] Unit test verifies inheritance
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests** (new file: `src/TurboHttp.Tests/RFC9114/01_QuicOptionsTests.cs`):

| DisplayName | Method |
|-------------|--------|
| `RFC9114-3.2-QO-001: QuicOptions inherits TcpOptions` | `Should_InheritTcpOptions_When_Created` |
| `RFC9114-3.2-QO-002: Default ALPN is h3` | `Should_HaveH3Alpn_When_Default` |
| `RFC9114-3.2-QO-003: IdleTimeout defaults to 30s` | `Should_Have30sIdle_When_Default` |

**Effort**: 1h

---

### TASK-002: QuicClientProvider

**Description:** As a library developer, I want a `QuicClientProvider` implementing `IClientProvider` so that QUIC connections return a `Stream`.

**Implementation**:
- New file: `src/TurboHttp/Transport/QuicClientProvider.cs`
  - `public sealed class QuicClientProvider(QuicOptions options) : IClientProvider`
  - `GetStreamAsync()` creates `QuicConnection` via `System.Net.Quic`, opens bidirectional stream, returns `QuicStream` (which IS a `System.IO.Stream`)
  - `Close()` disposes connection
  - `RemoteEndPoint` from `QuicConnection.RemoteEndPoint`

**Acceptance Criteria:**
- [x] Implements `IClientProvider` interface
- [x] `GetStreamAsync()` returns a `Stream` (QuicStream)
- [x] Connection uses ALPN "h3"
- [x] SNI set from `options.Host`
- [x] Certificate validation callback forwarded from options
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests** (new file: `src/TurboHttp.Tests/RFC9114/02_QuicClientProviderTests.cs`):

| DisplayName | Method |
|-------------|--------|
| `RFC9114-3.2-QCP-001: Provider implements IClientProvider` | `Should_ImplementInterface` |
| `RFC9114-3.2-QCP-002: GetStreamAsync returns Stream` | `Should_ReturnStream_When_Connected` |
| `RFC9114-3.1-QCP-003: SNI extension sent` | `Should_SendSNI_When_Connecting` |

**Effort**: 4-6h

---

### TASK-003: Transport Selection — TcpOptionsFactory + ClientManager

**Description:** As a library developer, I want HTTP/3 requests to automatically select QUIC transport based on request version.

**Implementation**:
- File: `src/TurboHttp/Transport/TcpOptionsFactory.cs`
  - Change: When `request.Version == HttpVersion.Version30` → return `QuicOptions`
- File: `src/TurboHttp/Transport/ClientManager.cs`
  - Change: Add `QuicOptions quic => new QuicClientProvider(quic)` to switch expression (1 line)

**Acceptance Criteria:**
- [x] HTTP/3 request version produces QuicOptions
- [x] ClientManager creates QuicClientProvider for QuicOptions
- [x] TCP/TLS requests unchanged
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests** (extends existing + new file: `src/TurboHttp.Tests/RFC9114/03_TransportSelectionTests.cs`):

| DisplayName | Method |
|-------------|--------|
| `RFC9114-3.2-TS-001: HTTP/3 request produces QuicOptions` | `Should_ProduceQuicOptions_When_Http3Version` |
| `RFC9114-3.2-TS-002: HTTP/1.1 request still produces TcpOptions` | `Should_ProduceTcpOptions_When_Http11Version` |
| `RFC9114-3.2-TS-003: ClientManager creates QuicClientProvider` | `Should_CreateQuicProvider_When_QuicOptions` |

**Effort**: 2h

---

### TASK-004: QUIC Variable-Length Integer Codec

**RFC**: 9000 §16

**Description**: Implement QUIC variable-length integer encoding and decoding per RFC 9000 §16.

**Implementation**:
- New file: `src/TurboHttp/Protocol/RFC9114/QuicVarInt.cs`
  - `static int Encode(long value, ref Span<byte> buffer)` — returns bytes written
  - `static (long value, int consumed) Decode(ReadOnlySpan<byte> buffer)` — returns value and bytes consumed

**Acceptance Criteria:**
- [x] 1-byte encoding for values 0-63
- [x] 2-byte encoding for values 64-16383
- [x] 4-byte encoding for values up to 2^30-1
- [x] 8-byte encoding for values up to 2^62-1
- [x] Encode-Decode roundtrip preserves value
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests** (new file: `src/TurboHttp.Tests/RFC9114/04_QuicVarIntTests.cs`):

| DisplayName | Method |
|-------------|--------|
| `RFC9000-16-QV-001: 1-byte encoding for values 0-63` | `Should_Use1Byte_When_ValueUnder64` |
| `RFC9000-16-QV-002: 2-byte encoding for values 64-16383` | `Should_Use2Bytes_When_ValueUnder16384` |
| `RFC9000-16-QV-003: 4-byte encoding for values up to 2^30-1` | `Should_Use4Bytes_When_ValueUnder2Pow30` |
| `RFC9000-16-QV-004: 8-byte encoding for values up to 2^62-1` | `Should_Use8Bytes_When_ValueUnder2Pow62` |
| `RFC9000-16-QV-005: Encode-Decode roundtrip` | `Should_Roundtrip_When_EncodedDecoded` |
| `RFC9000-16-QV-006: Max value 2^62-1` | `Should_Handle_When_MaxValue` |

**Effort**: 3-4h

---

### TASK-005: HTTP/3 Error Code Enum

**RFC**: 9114 §8.1

**Description**: Define all RFC 9114 error codes as an enum.

**Implementation**: `src/TurboHttp/Protocol/RFC9114/Http3ErrorCode.cs`

**Acceptance Criteria:**
- [x] All RFC 9114 §8.1 error codes defined (H3_NO_ERROR through H3_ID_ERROR)
- [x] Enum values match RFC-specified integer values
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/05_ErrorCodeTests.cs` — 4 tests

**Effort**: 1h

---

### TASK-006: HTTP/3 Frame Type Hierarchy

**RFC**: 9114 §7

**Description**: Implement abstract frame base class and all 7 HTTP/3 frame types with serialization.

**Implementation**:
- New file: `src/TurboHttp/Protocol/RFC9114/Http3Frame.cs`
  - Abstract `Http3Frame` with `WriteTo(ref Span<byte>)`, `SerializedSize`
  - Subclasses: `Http3DataFrame`, `Http3HeadersFrame`, `Http3CancelPushFrame`, `Http3SettingsFrame`, `Http3PushPromiseFrame`, `Http3GoAwayFrame`, `Http3MaxPushIdFrame`

**Acceptance Criteria:**
- [x] Abstract `Http3Frame` base class with `WriteTo(ref Span<byte>)` and `SerializedSize`
- [x] Subclasses for all 7 frame types: DATA, HEADERS, CANCEL_PUSH, SETTINGS, PUSH_PROMISE, GOAWAY, MAX_PUSH_ID
- [x] Each frame type serializes correctly via `WriteTo`
- [x] `SerializedSize` matches actual serialized byte count
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/06_FrameTypeTests.cs` — 10 tests

**Effort**: 6-8h

---

### TASK-007: HTTP/3 Settings Parameter Registry

**RFC**: 9114 §7.2.4

**Description**: Define all RFC 9114 SETTINGS parameters with serialization.

**Implementation**: `src/TurboHttp/Protocol/RFC9114/Http3Settings.cs`

**Acceptance Criteria:**
- [x] All RFC 9114 §7.2.4 settings defined (SETTINGS_MAX_FIELD_SECTION_SIZE, etc.)
- [x] Settings serialization via QUIC variable-length integers
- [x] Unknown settings preserved (extension tolerance)
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/07_SettingsTests.cs` — 8 tests

**Effort**: 3-4h

---

### PHASE 2 — Frame Codec (Week 2-3)

### TASK-008: HTTP/3 Frame Encoder

**RFC**: 9114 §7

**Description**: Implement wire-format encoding for all HTTP/3 frame types.

**Acceptance Criteria:**
- [x] All 7 frame types serialize to correct wire format
- [x] Frame type and length use QUIC variable-length integer encoding
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/08_FrameEncoderTests.cs` — 10 tests

**Effort**: 4-6h

---

### TASK-009: HTTP/3 Frame Decoder

**RFC**: 9114 §7

**Description**: Implement stateful wire-format decoding for all HTTP/3 frame types with partial frame handling.

**Acceptance Criteria:**
- [x] All 7 frame types decoded from wire format
- [x] Partial frame handling (incomplete data returns need-more-data)
- [x] Unknown frame types skipped gracefully
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/09_FrameDecoderTests.cs` — 12 tests

**Effort**: 8-10h

---

### TASK-010: Frame Encoder-Decoder Roundtrip Tests

**Description**: Verify encode-decode symmetry for all frame types.

**Acceptance Criteria:**
- [x] Encode-Decode roundtrip for each frame type preserves all fields
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/10_FrameRoundTripTests.cs` — 17 tests

**Effort**: 2-3h

---

### PHASE 3 — Stream Management (Week 3-4)

### TASK-011: Stream Type Definitions

**RFC**: 9114 §6

**Description**: Define stream type identifiers (control, push, QPACK encoder/decoder).

**Acceptance Criteria:**
- [x] Enum/constants for control stream (0x00), push stream (0x01), QPACK encoder (0x02), QPACK decoder (0x03)
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/11_StreamTypeTests.cs` — 6 tests

**Effort**: 1-2h

---

### TASK-012: Control Stream State Machine

**RFC**: 9114 §6.2.1

**Description**: Implement control stream lifecycle: client opens exactly one, receives server's, validates SETTINGS.

**Acceptance Criteria:**
- [x] Client opens exactly one control stream per connection
- [x] SETTINGS frame sent as first frame on control stream
- [x] Receiving second control stream from server is a connection error
- [x] Control stream closure is a connection error
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/12_ControlStreamTests.cs` — 14 tests

**Effort**: 4-6h

---

### TASK-013: Bidirectional Stream Lifecycle

**RFC**: 9114 §6.1

**Description**: Implement request-response mapping to bidirectional streams with proper lifecycle.

**Acceptance Criteria:**
- [x] Request/response mapped to single bidirectional stream
- [x] Stream ID allocated from client range (even)
- [x] Half-close after request sent, full close after response received
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/13_BiStreamTests.cs` — 14 tests

**Effort**: 4-6h

---

### TASK-014: Unidirectional Stream Handling

**RFC**: 9114 §6.2

**Description**: Implement unidirectional stream type routing and unknown stream type tolerance.

**Acceptance Criteria:**
- [x] Server unidirectional streams identified by type byte
- [x] Unknown unidirectional stream types ignored (not error)
- [x] QPACK encoder/decoder streams routed correctly
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9114/14_UniStreamTests.cs` — 8 tests

**Effort**: 3-4h

---

### PHASE 4 — QPACK Codec (RFC 9204) (Week 4-5)

### TASK-015: QPACK Static Table

**RFC**: 9204 §3.1, Appendix A

**Description**: Implement QPACK static table with 99 entries.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackStaticTable.cs`

**Acceptance Criteria:**
- [x] All 99 static table entries from RFC 9204 Appendix A
- [x] Lookup by index returns correct name-value pair
- [x] Lookup by name returns correct index
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/01_StaticTableTests.cs` — 6 tests

**Effort**: 3-4h

---

### TASK-016: QPACK Integer Representation

**RFC**: 9204 §4.1.1

**Description**: Implement variable-length integer encoding for QPACK with multiple prefix lengths.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackIntegerCodec.cs`

**Acceptance Criteria:**
- [x] Encode/decode with various prefix lengths (5, 6, 7, 8 bits)
- [x] Values requiring multi-byte encoding handled correctly
- [x] Roundtrip preserves value for all prefix lengths
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/02_IntegerCodecTests.cs` — 8 tests

**Effort**: 2-3h

---

### TASK-017: QPACK String Literal Representation

**RFC**: 9204 §4.1.2

**Description**: Implement plain and Huffman-encoded string literals, reusing existing HuffmanCodec.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackStringCodec.cs`

**Acceptance Criteria:**
- [x] Plain string encoding/decoding
- [x] Huffman-encoded string encoding/decoding (reuses existing `HuffmanCodec`)
- [x] Empty string handled
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/03_StringCodecTests.cs` — 6 tests

**Effort**: 2-3h

---

### TASK-018: QPACK Dynamic Table

**RFC**: 9204 §3.2

**Description**: Implement dynamic table with absolute indexing, insert count tracking, and capacity management.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackDynamicTable.cs`

**Acceptance Criteria:**
- [x] Absolute indexing (not relative like HPACK)
- [x] Insert count tracking
- [x] Capacity management and eviction
- [x] 32-byte per-entry overhead applied
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/04_DynamicTableTests.cs` — 8 tests

**Effort**: 4-6h

---

### TASK-019: Encoder Instruction Stream Writer

**RFC**: 9204 §4.3

**Description**: Implement encoder instructions: Set Dynamic Table Capacity, Insert With/Without Name Reference, Duplicate.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackEncoderInstructionWriter.cs`

**Acceptance Criteria:**
- [x] Set Dynamic Table Capacity instruction
- [x] Insert With Name Reference instruction
- [x] Insert With Literal Name instruction
- [x] Duplicate instruction
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/05_EncoderInstructionTests.cs` — 8 tests

**Effort**: 4-6h

---

### TASK-020: Decoder Instruction Stream Writer

**RFC**: 9204 §4.4

**Description**: Implement decoder instructions: Section Acknowledgment, Stream Cancellation, Insert Count Increment.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackDecoderInstructionWriter.cs`

**Acceptance Criteria:**
- [x] Section Acknowledgment instruction
- [x] Stream Cancellation instruction
- [x] Insert Count Increment instruction
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/06_DecoderInstructionTests.cs` — 6 tests

**Effort**: 3-4h

---

### TASK-021: Instruction Stream Parser

**RFC**: 9204 §4.3, §4.4

**Description**: Implement parsing for all encoder and decoder instructions with partial frame handling.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackInstructionDecoder.cs`

**Acceptance Criteria:**
- [x] All encoder instructions decoded
- [x] All decoder instructions decoded
- [x] Partial instruction handling (need more data)
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/07_InstructionDecoderTests.cs` — 8 tests

**Effort**: 4-6h

---

### TASK-022: QPACK Header Block Encoder

**RFC**: 9204 §4.5

**Description**: Implement QPACK header block encoding with static table, dynamic table, and literal support.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackEncoder.cs`

**Acceptance Criteria:**
- [x] Static table indexed headers
- [x] Dynamic table insert + reference
- [x] Literal headers without indexing
- [x] NEVERINDEX for sensitive headers (Authorization, Cookie, etc.)
- [x] Required Insert Count prefix
- [x] Huffman encoding support
- [x] Encoder instructions emitted as side-effect
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/08_QpackEncoderTests.cs` — 10 tests

**Effort**: 10-14h

---

### TASK-023: QPACK Header Block Decoder

**RFC**: 9204 §4.5

**Description**: Implement QPACK header block decoding with required insert count validation and blocked stream handling.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackDecoder.cs`

**Acceptance Criteria:**
- [x] All 5 encoding types decoded (indexed, indexed post-base, literal name ref, literal post-base ref, literal)
- [x] Required Insert Count validation
- [x] Blocked stream handling (SETTINGS_QPACK_BLOCKED_STREAMS)
- [x] Huffman decoding support
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/09_QpackDecoderTests.cs` — 10 tests

**Effort**: 10-14h

---

### TASK-024: QPACK Encode-Decode Roundtrip

**Description**: Verify header list encoding-decoding preserves all name-value pairs and dynamic table state.

**Acceptance Criteria:**
- [x] Header list roundtrip through encode→decode preserves all name-value pairs
- [x] Static-only encoding roundtrips
- [x] Dynamic table entries roundtrip
- [x] Sensitive headers (NEVERINDEX) roundtrip
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/10_QpackRoundTripTests.cs` — 8 tests

**Effort**: 3-4h

---

### TASK-025: QPACK Table Synchronization

**RFC**: 9204 §2.1.1, §2.1.2

**Description**: Implement encoder-decoder dynamic table synchronization via instruction streams.

**Implementation**: `src/TurboHttp/Protocol/RFC9204/QpackTableSync.cs`

**Acceptance Criteria:**
- [x] Encoder and decoder dynamic tables stay synchronized
- [x] Insert Count tracked across multiple header blocks
- [x] Blocked streams resolved when required insert count reached
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.Tests/RFC9204/11_TableSyncTests.cs` — 8 tests

**Effort**: 6-8h

---

### TASK-026: QPACK → HTTP/3 Integration

**Description**: Integrate QpackEncoder and QpackDecoder into Http3RequestEncoder and Http3ResponseDecoder.

**Implementation**:
- Modifies `src/TurboHttp/Protocol/RFC9114/Http3RequestEncoder.cs` — uses QpackEncoder
- Modifies `src/TurboHttp/Protocol/RFC9114/Http3ResponseDecoder.cs` — uses QpackDecoder

**Acceptance Criteria:**
- [x] `Http3RequestEncoder` uses `QpackEncoder` for header compression
- [x] `Http3ResponseDecoder` uses `QpackDecoder` for header decompression
- [x] Integration tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: 12 integration tests

**Effort**: 4-6h

---

### TASK-027: QPACK Stream Stages

**RFC**: 9204 (streaming integration)

**Description**: Implement Akka stream stages for QPACK encoder/decoder instruction streams.

**Implementation**:
- `src/TurboHttp/Streams/Stages/Encoding/QpackEncoderStreamStage.cs`
  - Port names: `QpackEncoder.In`, `QpackEncoder.Out`
- `src/TurboHttp/Streams/Stages/Decoding/QpackDecoderStreamStage.cs`
  - Port names: `QpackDecoder.In`, `QpackDecoder.Out`

**Acceptance Criteria:**
- [x] `QpackEncoderStreamStage` with ports `QpackEncoder.In`, `QpackEncoder.Out`
- [x] `QpackDecoderStreamStage` with ports `QpackDecoder.In`, `QpackDecoder.Out`
- [x] Stage tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Tests**: `src/TurboHttp.StreamTests/RFC9204/QpackStreamStageTests.cs` — 6 tests

**Effort**: 4-6h

---

### PHASE 5 — HTTP Semantics Mapping (Week 5-6, requires Phase 4)

### TASK-028: Pseudo-Header Validation

**RFC**: 9114 §4.3

**Description**: Validate pseudo-headers in requests and responses.

**Acceptance Criteria:**
- [x] `:method`, `:scheme`, `:authority`, `:path` validated for requests
- [x] `:status` validated for responses
- [x] Unknown pseudo-headers rejected
- [x] Pseudo-headers after regular headers rejected
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 4-6h

---

### TASK-029: Field Name/Value Validation

**RFC**: 9114 §4.2

**Description**: Validate field names and values per HTTP/3 requirements.

**Acceptance Criteria:**
- [x] Uppercase field names rejected
- [x] Connection-specific headers rejected (Connection, Transfer-Encoding, etc.)
- [x] TE header allowed only with value "trailers"
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 3-4h

---

### TASK-030: Http3RequestEncoder

**RFC**: 9114 §4.1, §4.3.1

**Description**: Implement HttpRequestMessage → Http3Frame encoding with QPACK and pseudo-headers.

**Acceptance Criteria:**
- [x] Request mapped to HEADERS frame + optional DATA frames
- [x] Pseudo-headers (:method, :scheme, :authority, :path) set correctly
- [x] QPACK encoding applied to header block
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 6-8h

---

### TASK-031: Http3ResponseDecoder

**RFC**: 9114 §4.1, §4.3.2

**Description**: Implement Http3Frame → HttpResponseMessage decoding with QPACK and body assembly.

**Acceptance Criteria:**
- [x] HEADERS frame decoded to HttpResponseMessage
- [x] DATA frames assembled into response body
- [x] QPACK decoding applied to header block
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 6-8h

---

### TASK-032: CONNECT Method Handling

**RFC**: 9114 §4.4

**Description**: Handle CONNECT requests with tunnel semantics.

**Acceptance Criteria:**
- [x] CONNECT request uses :authority pseudo-header only (no :scheme, :path)
- [x] Successful CONNECT response establishes tunnel
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 3-4h

---

### PHASE 6 — Connection Management (Week 6-7)

### TASK-033: GOAWAY Processing

**RFC**: 9114 §5.2, §7.2.6

**Description**: Implement GOAWAY frame parsing and graceful connection shutdown.

**Acceptance Criteria:**
- [x] GOAWAY frame parsed and last stream ID extracted
- [x] Requests with stream ID > last stream ID retried on new connection
- [x] Client sends GOAWAY before closing connection
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 4-6h

---

### TASK-034: SETTINGS Frame Exchange

**RFC**: 9114 §7.2.4, §7.2.4.2

**Description**: Implement SETTINGS frame exchange at connection start.

**Acceptance Criteria:**
- [x] Client sends SETTINGS as first frame on control stream
- [x] Server SETTINGS parsed and applied
- [x] SETTINGS_MAX_FIELD_SECTION_SIZE enforced
- [x] HTTP/2 settings (forbidden in HTTP/3) rejected
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 4-6h

---

### TASK-035: Connection Reuse + Certificate Validation

**RFC**: 9114 §3.1, §3.3

**Description**: Implement connection pooling and certificate validation for connection reuse.

**Acceptance Criteria:**
- [x] Connection reused for same origin
- [x] Certificate covers requested hostname
- [x] Mismatched certificate forces new connection
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 3-4h

---

### TASK-036: Idle Timeout + Reconnection

**RFC**: 9114 §5.1

**Description**: Implement idle timeout enforcement and automatic reconnection.

**Acceptance Criteria:**
- [x] Idle connections closed after timeout
- [x] Reconnection attempted on connection loss
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### PHASE 7 — Push Management (Week 7)

### TASK-037: MAX_PUSH_ID Frame

**RFC**: 9114 §7.2.7

**Description**: Implement MAX_PUSH_ID frame to limit server push.

**Acceptance Criteria:**
- [x] MAX_PUSH_ID frame sent to control server push limits
- [x] Push IDs beyond MAX_PUSH_ID rejected
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### TASK-038: PUSH_PROMISE Validation

**RFC**: 9114 §4.6, §7.2.5

**Description**: Validate PUSH_PROMISE frames and promised request headers.

**Acceptance Criteria:**
- [x] PUSH_PROMISE decoded and validated
- [x] Push ID within allowed range
- [x] Duplicate push ID rejected
- [x] Promised request headers validated
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 4-6h

---

### TASK-039: CANCEL_PUSH Frame

**RFC**: 9114 §7.2.3

**Description**: Implement CANCEL_PUSH for declining unwanted server pushes.

**Acceptance Criteria:**
- [x] CANCEL_PUSH sent to cancel unwanted server push
- [x] CANCEL_PUSH for unknown push ID is not an error
- [x] Unit tests written and passing
- [x] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### PHASE 8 — Akka.Streams Integration (Week 7-9)

### TASK-040: Http30EncoderStage

**Description**: Implement Akka stage for HTTP/3 frame serialization.

**Acceptance Criteria:**
- [ ] Port names: `Http30Encoder.In`, `Http30Encoder.Out`
- [ ] Serializes Http3Frame to bytes
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 3-4h

---

### TASK-041: Http30DecoderStage

**Description**: Implement Akka stage for HTTP/3 frame deserialization.

**Acceptance Criteria:**
- [ ] Port names: `Http30Decoder.In`, `Http30Decoder.Out`
- [ ] Parses bytes to Http3Frame
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 3-4h

---

### TASK-042: Http30StreamStage

**Description**: Implement Akka stage to assemble frames into HttpResponseMessage.

**Acceptance Criteria:**
- [ ] Port names: `Http30Stream.In`, `Http30Stream.Out`
- [ ] Assembles HEADERS + DATA frames into HttpResponseMessage
- [ ] QPACK decoding and decompression applied
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 6-8h

---

### TASK-043: Http30ConnectionStage

**Description**: Implement Akka stage for control stream and connection-level frame handling.

**Acceptance Criteria:**
- [ ] Port names: `H3Connection.In.App`, `H3Connection.Out.App`, `H3Connection.In.Server`, `H3Connection.Out.Server`
- [ ] Control stream managed (SETTINGS/GOAWAY)
- [ ] Flow control enforcement
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 8-10h

---

### TASK-044: Http3Request2FrameStage

**Description**: Implement Akka stage to convert HttpRequestMessage to list of Http3Frames.

**Acceptance Criteria:**
- [ ] Converts HttpRequestMessage to list of Http3Frames
- [ ] HEADERS frame with QPACK-encoded headers
- [ ] DATA frames for request body
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 4-6h

---

### TASK-045: Http30Engine

**RFC**: 9114

**Description**: Implement full HTTP/3 pipeline in Http30Engine.

**Acceptance Criteria:**
- [ ] Replaces `NotSupportedException` stub in `Http30Engine.cs`
- [ ] Full HTTP/3 pipeline assembled (encoder → connection → decoder stages)
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 8-10h

---

### TASK-046: Engine HTTP/3 Routing

**RFC**: 9114 (integration)

**Description**: Update Engine.cs to route HTTP/3 requests to Http30Engine.

**Acceptance Criteria:**
- [ ] `Engine.cs` routes HTTP/3 requests to `Http30Engine`
- [ ] HTTP/1.x and HTTP/2 routing unchanged
- [ ] Stage tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### PHASE 9 — Security & Extensions (Week 9-10)

### TASK-047: SNI TLS Enforcement

**RFC**: 9114 §3.2

**Description**: Enforce SNI extension in TLS handshake for QUIC.

**Acceptance Criteria:**
- [ ] SNI extension sent in TLS handshake for QUIC connections
- [ ] Missing SNI triggers connection error
- [ ] Unit tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 3-4h

---

### TASK-048: Intermediary Encapsulation Attack Prevention

**RFC**: 9114 §10.3

**Description**: Reject invalid field names and prohibited target origins.

**Acceptance Criteria:**
- [ ] Reject field names containing characters invalid in HTTP/3
- [ ] Reject requests targeting prohibited origins
- [ ] Unit tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### TASK-049: DoS Prevention — Push Limiting

**RFC**: 9114 §10.5

**Description**: Enforce push limits via MAX_PUSH_ID.

**Acceptance Criteria:**
- [ ] MAX_PUSH_ID limits enforced
- [ ] Excessive push promises trigger connection error
- [ ] Unit tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### TASK-050: Extension Tolerance

**RFC**: 9114 §9

**Description**: Gracefully handle unknown frames and settings.

**Acceptance Criteria:**
- [ ] Unknown frame types ignored (not connection error)
- [ ] Unknown settings ignored (not connection error)
- [ ] Reserved frame types (0x21 pattern) handled correctly
- [ ] Unit tests written and passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 2-3h

---

### TASK-051: Integration Tests

**RFC**: 9114 (full end-to-end)

**Description**: End-to-end integration tests with real QUIC connections.

**Acceptance Criteria:**
- [ ] End-to-end HTTP/3 request/response test
- [ ] Multi-stream concurrent requests test
- [ ] GOAWAY graceful shutdown test
- [ ] Server push receive/cancel test
- [ ] All integration tests passing
- [ ] `dotnet build --configuration Release src/TurboHttp.sln` → 0 errors

**Effort**: 12-16h

---

## Non-Goals

- 0-RTT QUIC (post-MVP)
- Server push acceptance (framing only)
- HTTP/3 server implementation
- WebSocket/WebTransport over HTTP/3
- `QuicConnectionStage` — NOT NEEDED (existing `ConnectionStage` works via `IClientProvider`)
- QPACK server-side encoding (client encode + decode only)
- QPACK without HTTP/3 (not standalone)
- Backwards compatibility with HPACK

## Effort Summary

| Phase | Tasks | Hours |
|-------|-------|-------|
| 1: Foundation (transport + codecs) | TASK-001-007 | 20-29h |
| 2: Frame Codec | TASK-008-010 | 14-19h |
| 3: Stream Management | TASK-011-014 | 12-18h |
| 4: QPACK Codec (RFC 9204) | TASK-015-027 | 59-84h |
| 5: Semantics (requires Phase 4) | TASK-028-032 | 22-30h |
| 6: Connection Management | TASK-033-036 | 13-19h |
| 7: Push Management | TASK-037-039 | 8-12h |
| 8: Akka Integration | TASK-040-046 | 34-45h |
| 9: Security + Integration | TASK-047-051 | 21-29h |
| **Total** | **51 tasks** | **203-285h** |

## Compliance Table

| Metric | Before | After Phase 1-3 | After Phase 4-7 | After Phase 8-9 |
|--------|--------|----------------|----------------|----------------|
| Implemented | 2/145 | ~45/145 | ~115/145 | **143/145** |
| Score | 1.4% | ~31% | ~79% | **99%** |

**Remaining 2 items**: 0-RTT settings compliance (§7.2.4.2) — deferred to post-MVP.

## Open Questions

1. System.Net.Quic stability on Windows in .NET 10?
2. libmsquic dependency on Linux — how to package?
3. QUIC connection migration support needed?
4. HTTP/3 Alt-Svc discovery — automatic or manual only?
