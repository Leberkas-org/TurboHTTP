name: Docs Build

permissions:
  contents: read

on:
  pull_request:
    branches: ["main"]
    paths:
      - "docs/**"

jobs:
  docs:
    uses: Leberkas-org/template.pipeline.github/.github/workflows/docs-build.yml@v0
